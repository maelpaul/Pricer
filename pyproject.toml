[project]
name = "skema-pricer"
version = "1.0.0"
description = "Skema Pricer — Interactive derivatives pricer built with Streamlit"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.30.0",
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "plotly>=5.15.0",
    "pandas>=2.0.0",
]
